import type { Product } from '../../types/product'
const render = (value: unknown) => Array.isArray(value) ? value.map((v, i) => <li key={i}>{typeof v === 'object' ? JSON.stringify(v) : String(v)}</li>) : value && typeof value === 'object' ? <pre className="whitespace-pre-wrap text-xs">{JSON.stringify(value, null, 2)}</pre> : value ? <p>{String(value)}</p> : null
export function ProductDescription({ product }: { product: Product }) { return <section className="space-y-5 p-4"><div><h2 className="font-semibold">Description</h2><p className="mt-2 text-sm leading-6 text-neutral-600">{product.description}</p></div><div><h2 className="font-semibold">Main features</h2><ul className="mt-2 list-disc space-y-1 pl-5 text-sm text-neutral-600">{render(product.main_features)}</ul></div><div><h2 className="font-semibold">Additional features</h2><ul className="mt-2 list-disc space-y-1 pl-5 text-sm text-neutral-600">{render(product.sub_features)}</ul></div></section> }
