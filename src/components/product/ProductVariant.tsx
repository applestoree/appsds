import type { Product } from '../../types/product'
export function ProductVariant({ product }: { product: Product }) { const colors = Array.isArray(product.variant_color) ? product.variant_color : []; const sizes = Array.isArray(product.variant_size) ? product.variant_size : []; return <section className="border-y border-black/5 p-4"><h2 className="font-semibold">Variants</h2>{colors.length > 0 && <div className="mt-3"><div className="text-xs text-neutral-500">Color</div><div className="mt-2 flex flex-wrap gap-2">{colors.map((v, i) => <span key={i} className="rounded-full border px-3 py-1 text-xs">{typeof v === 'object' ? JSON.stringify(v) : String(v)}</span>)}</div></div>}{sizes.length > 0 && <div className="mt-4"><div className="text-xs text-neutral-500">Variant</div><div className="mt-2 flex flex-wrap gap-2">{sizes.map((v, i) => <span key={i} className="rounded-full border px-3 py-1 text-xs">{typeof v === 'object' ? JSON.stringify(v) : String(v)}</span>)}</div></div>}{colors.length === 0 && sizes.length === 0 && <p className="mt-2 text-sm text-neutral-500">No variant data available.</p>}</section> }
