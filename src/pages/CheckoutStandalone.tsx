import { Header } from '../components/layout/Header'
import { BottomActionBar } from '../components/layout/BottomActionBar'
import { Button } from '../components/common/Button'
import { useCart } from '../hooks/useCart'

export function CheckoutStandalone() { const { items, updateQuantity } = useCart(); return <div className="mx-auto flex min-h-[100dvh] w-full max-w-[500px] flex-col bg-white"><Header standalone /><main className="min-h-0 flex-1 overflow-y-auto p-4 pb-24"><h1 className="text-2xl font-semibold">Checkout</h1><section className="mt-5 space-y-3"><h2 className="font-semibold">Delivery Address</h2><div className="rounded-2xl bg-neutral-100 p-4 text-sm text-neutral-500">Add delivery address</div></section><section className="mt-5 space-y-3"><h2 className="font-semibold">Store</h2><div className="rounded-2xl bg-neutral-100 p-4">Apple Store Malaysia</div></section><section className="mt-5 space-y-3"><h2 className="font-semibold">Shipping</h2><div className="rounded-2xl border p-4 text-sm">Delivery / Store Pickup</div></section><section className="mt-5 space-y-3"><h2 className="font-semibold">Payment</h2><div className="rounded-2xl border p-4 text-sm">Bank Transfer / DuitNow QR</div></section><section className="mt-5"><h2 className="font-semibold">Items</h2>{items.length === 0 ? <div className="py-6 text-sm text-neutral-500">Your cart is empty.</div> : <div className="mt-3 space-y-3">{items.map(item => <div key={item.product.item_group_id} className="flex items-center gap-3 rounded-2xl bg-neutral-50 p-3"><img src={item.product.link} alt="" className="h-16 w-16 object-contain" /><div className="min-w-0 flex-1"><div className="truncate text-sm font-medium">{item.product.title}</div><div className="mt-1 flex items-center gap-2"><button onClick={() => updateQuantity(item.product.item_group_id, item.quantity - 1)} className="rounded-full bg-white px-2">−</button><span className="text-xs">{item.quantity}</span><button onClick={() => updateQuantity(item.product.item_group_id, item.quantity + 1)} className="rounded-full bg-white px-2">+</button></div></div></div>)}</div>}</section><section className="mt-5"><h2 className="font-semibold">Order Summary</h2><div className="mt-3 rounded-2xl bg-neutral-100 p-4 text-sm">Product totals are calculated from selected API variant data when pricing is available.</div></section></main><BottomActionBar><Button className="w-full" disabled={!items.length}>Place Order</Button></BottomActionBar></div> }
