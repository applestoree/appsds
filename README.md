# Apple Store Malaysia — Frontend

Mobile-only React + TypeScript + Vite + Tailwind CSS application.

Product data is consumed only through the `app-products` Supabase Edge Function. See `docs/app-products.md` for the API contract.

## Commands

```bash
npm install
npm run dev
npm run build
```
